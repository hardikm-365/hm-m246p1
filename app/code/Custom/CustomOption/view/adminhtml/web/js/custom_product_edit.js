require([
    'jquery'
], function ($) {
    'use strict';
   $(document).ready(function(){
   	// var licount = $("input[name='product[options]']").length;
   	// alert(licount);
   	// $(document).on('change',"input[name='product[options][0][values][0][image]']",function() {
   	// 	alert($(this).val());

   	// });
  
});
});